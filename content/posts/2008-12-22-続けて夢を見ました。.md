---
title: 続けて夢を見ました。
date: 2008-12-22T00:19:31+00:00
draft: false
categories:
  - OSX86
tags:
  - boot-132
  - DSDT Patcher
  - DSDT.aml
  - EFI boot v6.1
  - GUIDパーティション
  - Leopard
  - Mac
  - OSX86
---
[EFI boot v6](http://forum.insanelymac.com/index.php?showtopic=127330) を落として、USBデバイス経由でなく、スタンドアローンで起動するようにしてみました。この、EFI boot は、Boot-132にインスパイヤされたものだと作成者自身が記事で述べているように、GUIDパーティションに必ずついてくる EFIパーティションにブートローダやら、kextを突っ込んでしまえという方法です。元々 Apple はここを使ってないので、昨今の風潮にあわせた資源の有効活用ってやつですね。やり方はごっつ簡単。Leopard 導入済みの環境でも、DVDのインストーラから起動した環境でもインストール可能です。

**■必要なもの**

  1.    [efi_boot_v6.1.zip](http://forum.insanelymac.com/index.php?s=098b3ca4522e4d535357b5d759060f18&act=attach&type=post&id=39402">) を InsanelyMac の [[Guide] Boot from EFI partition, zero modification installs on Intel SSE2 or better&#8230;, Zero-modification Leopard installs. Share disks between macs & ha](http://forum.insanelymac.com/index.php?showtopic=127330) ってエントリから落とす。
  1.  zip を展開しておく。
  1.  DSDT Pacher で、<span style="color:#ff0000;">DSDT</span>.aml を造っておく。

**[1] インストール先ディスクのデバイス名を取得する**

   1.  ターミナルを開き、sudo -s でルートになっておく。
   1. レパード実行環境なら、diskutil info / | grep Identifier を実行。表示されたディスク名(disk0s2 とか)をメモっとく。
   1. DVD起動なら、diskutil info /Volume/なんとか | grep Identifier。/Volume/なんとか は、対象のディスクのマウントポイント。

**[2] EFIパーティションを初期化する**

   1. さて、次に、EFIパーティションを使えるようにしなくてはなりません。通常、FAT32でフォーマットされているので、これを HFS+ に置き換えるわけです。そこで、次のコマンドを実行。
   1. diskutil eraseVolume &#8220;HFS+&#8221; &#8220;EFI&#8221; /dev/diskXs1 (diskXs1 の X は、[1]で取得したディスク名の最初の数字)

**[3] ディスクを起動可能にする**

   1. そのままでは、OSX86 をインストールした パーティションが、active (起動可能) になっているので、EFIパーティションが、active になるようおまじないをします。
   1. ./fdisk -e /dev/rdiskX (rdiskX の X は、[1]で取得したディスク名の最初の数字)
   1. すると、fdisk: could not open MBR file /usr/standalone/i386/boot0: No such file or directory なんてメッセージが出ますが、無視します。
   1. fdisk: X> とプロンプトが出ますので、f 1 と入力し、リターン。
   1. Partition 1 marked active. なんてメッセージが出力されます。
   1. 続いて、w と入力してリターン。これで、4 で変更した内容が書き込まれます。
   1. q と入力してリターン。で、終了。

**[4] インストールする**

   1. EFI Boot v6.1 を展開したディレクトリへ cd して、まずイニシャルブートローダを書き込む。
   1. と、その前に、コマンドに実行属性がなかったりするので、chmod +x fdisk とおまじないしておく。
   1. ./fdisk -f boot0 -u -y /dev/rdiskX を実行。(rdiskX の X は、[1]でメモったディスク名の最初の数字を指定して下さい)
   1. dd if=boot1h of=/dev/rdiskXs1 を実行。(最後の s1 が重要です。ここを変えたりしちゃいけません)
   1. mkdir /Volume/EFI
   1. mount_hfs /dev/diskXs1 /Volume/EFI (diskXs1 の X は、[1]でメモったディスク名の最初の数字を指定して下さい)
   1. cp boot-turbo-munky.bin /Volumes/EFI/<span style="color:#ff0000;">boot</span>
   1. cp update.sh /Volume/EFI/
   1. mkdir -p /Volumes/EFI/System/Booter
   1. mkdir /Volumes/EFI/Extensions
   1. mkdir /Volumes/EFI/.fseventsd (既にあったら造らなくてもよい)
   1. touch /Volumes/EFI/.fseventsd/no_log
   1. 必要な kext を /Volume/Extensions/ にコピーする。P5E WS PRO の場合は、次の通り。
   1. AppleAHCISupport.kext, AppleIntelPIIXATASupport.kext, SMBIOSEnabler.kext, dsmos.kext, AppleAHCIPort.kext, AppleYukon2Support.kext, AppleVIAATA.kext, OpenHaltRestart.kext (DSDT Patch を使用するので、Disabler系は不要)
   1. <span style="color:#ff0000;">DSDT</span>.aml を /Volume/EFI へコピーする。
   1. /Library/Preferences/SystemConfiguration/com.apple.Boot.plist を /Volume/EFI へコピーする。
   1. cd /Volume/EFI; chmod +x update.sh
   1. ./update.sh を実行する。警告が表示されても気にしない。
   1. cd .. して、umount /Volume/EFI で、アンマウントする。できない場合は、-f オプションをつけて強制アンマウント。
   1. 以上でインストール終了!

いざ、再起動です。kextが読まれるのが、暫し画面に表示された後、あの灰色の画面が&#8230;って、林檎マークが出ない&#8230;ま、そういう仕様なんですが、ちょっと寂しい。まあ、起動してしまえば、問題なく使えます。リブートとシャットダウンがシャキシャキと反応するようになったんで、DSDT Patch もちゃんと効いてるみたい。相変わらず sleep が復帰してこないんですが、うちの場合は許容範囲です。

